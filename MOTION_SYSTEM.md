# OpenArc OS 交互动效系统

版本：动效提案 0.1，2026-09-10。与 DESIGN_SYSTEM.md 和 PLAN.md 配套使用。
方向遵循 [Spectrum UI](https://ui.spectrumhq.in/)，数值为初始建议，不是已完成效果或 Apple 官方参数。
动效解释状态、空间和因果，不制造未发生的加载进度或完成感。

## 1. 动效职责与边界

反馈动效确认点击或状态变化，空间动效说明窗口从哪里来、到哪里去。
持续动效只用于确有持续工作的状态，空闲桌面不长期漂浮或闪烁。
应用业务状态由服务事件决定，动画结束不能把任务改成成功。
用户输入优先于装饰动画，快速点击和反向操作应立即响应。
不为每个组件独立发明弹性参数，整体采用统一曲线与少量语义时长。
自然语言操作和手动操作使用相同结果动效，AI 不享有绕过权限的视觉通道。
动效不能挡住键盘焦点、影响用户读取或掩盖失败原因。

## 2. 动效 token 初始方案

| Token | 初始值 | 使用情形 |
| --- | --- | --- |
| duration.instant | 0 ms | 减少动态效果、紧急状态切换 |
| duration.feedback | 100 ms | 按下、颜色、短反馈 |
| duration.quick | 160 ms | 菜单、提示、悬停离开 |
| duration.standard | 220 ms | 对话框、抽屉、通知 |
| duration.spatial | 300 ms | 窗口收起恢复、尺寸转换 |
| duration.long | 380 ms 上限建议 | 较大空间变化，需说明理由 |
| easing.standard | cubic-bezier(0.2,0,0,1) | 常规进入与位置调整 |
| easing.exit | cubic-bezier(0.4,0,1,1) | 离开当前层 |
| easing.linear | linear | 已知进度和匀速指示 |

弹性只用于 Dock 与可拖动对象回位，统一使用 motion.spring.gentle 语义名。
具体弹簧参数待 D1 按选用库校准；不同库的 stiffness、damping 不直接互抄。
如无法保证稳定中断，先使用 standard 曲线，不为了弹性牺牲输入响应。
弹性目标是不明显过冲、无多次回弹，停止输入后建议 300 ms 内稳定，需实测。
同一交互共享时间基准，图标、背景和阴影不无故错开。
长距离移动不无限拉长时长，超过 380 ms 的非必要移动应改为更短过渡。

## 3. 统一中断与焦点规则

每个动画持有当前视觉值和目标状态，反向操作从当前值开始，不先跳回起点。
进入过程中关闭就转向退出；退出中重新打开就转回进入，不叠加两个动画。
动画结束回调须核对最新状态，旧回调不能隐藏已重新打开的窗口。
正在离开的层立即停止接收新的业务点击，但保留必要的焦点转移处理。
进入时交互区域与视觉位置一致，禁止出现按钮已移动而命中区仍在原处。
模态层打开先设置焦点边界，再完成视觉过渡；关闭后归还触发焦点。
菜单可立即响应键盘，不要求用户等动画结束。
窗口最小化先提交窗口状态，恢复布局状态与动画状态分开记录。
取消任务不会反向播放已成功步骤；仅更新真实取消状态。

## 4. 桌面与窗口动效规格

位移单位为逻辑 px；起止值是提案，原生视图可行性验证后冻结。

| 交互与触发 | 起止值 | 初始时长 | 中断、点击与焦点 |
| --- | --- | --- | --- |
| 启动窗口 | opacity 0→1，y 8→0，scale .98→1 | 220 ms | 可关闭反向；激活后焦点进入窗口 |
| 关闭窗口 | opacity 1→0，scale 1→.985 | 160 ms | 未保存处理先完成；退出停止业务点击 |
| 激活窗口 | 边框与阴影非激活→激活 | 100 ms | 焦点立即切换，不等阴影 |
| 最小化 | 窗口矩形→Dock 目标或简化淡出 | 300 ms | 从当前矩形反向；移交焦点 |
| 恢复 | Dock 目标→已保存窗口矩形 | 300 ms | 可再次最小化；最新状态唯一有效 |
| 最大化 | 当前边界→工作区边界 | 300 ms | 原生网页未通过时采用即时布局加淡变 |
| 还原 | 工作区→恢复边界 | 300 ms | 不重新计算随机位置 |
| 窗口拖动 | 位置紧随指针，无时间补间 | 0 ms | 不放大正文；释放后必要位置校正 |
| 窗口缩放 | 边界紧随指针 | 0 ms | 原生视图同步裁剪，不能延迟命中区 |
| 多屏归位 | 不可见窗口→可见工作区 | 160 ms 或即时 | 先确保可见，不进行跨屏长飞行 |
| Dock 悬停 | scale 1→1.08，y 0→-3 | 160 ms 或 gentle | 退出从当前值回位；不挤动命中区 |
| Dock 启动状态 | 静态标记→加载指示 | 100 ms | 不无限弹跳；失败转可查看提示 |

最小化矩形变换只在原生浏览器层与桌面一致合成时启用，不能用假截图掩盖长期不可用视图。
最大化、缩放期间必须测试网页定位、裁剪、点击和高像素密度，不只录装饰动画。
如使用原生窗口动画，避免再套一层网页缩放形成双重动效。

## 5. 浮层、导航与列表

| 交互与触发 | 起止值 | 初始时长 | 中断、点击与焦点 |
| --- | --- | --- | --- |
| Menu 打开 | opacity 0→1，y -4→0 | 160 ms | 方向键立即可用；Escape 反向退出 |
| Popover 打开 | opacity 0→1，scale .98→1 | 160 ms | 以锚点为原点，边缘避让先计算 |
| Dialog 打开 | opacity 0→1，y 8→0 | 220 ms | 先建立模态焦点边界 |
| Dialog 关闭 | opacity 1→0，y 0→4 | 160 ms | 焦点回触发者；旧回调不可误关新层 |
| Drawer 打开 | x 从侧边 24→0，opacity 0→1 | 220 ms | 不要求整屏长滑行；模态属性固定 |
| Tree 展开 | 子项可见高度 0→内容高度 | 160 ms | 大树采用即时展开，避免大量布局补间 |
| Tabs 切换 | 指示线位置旧→新，内容轻淡变 | 160 ms | 内容焦点遵守标签键盘规则 |
| 页面切换 | opacity .6→1，y 4→0 | 160 ms | 保留滚动状态，不每次清空页面 |
| 列表插入 | opacity 0→1，y 4→0 | 160 ms | 只动画新增可见行 |
| 列表删除 | opacity 1→0，后续行移位 | 160 ms | 删除先处理业务权限与恢复规则 |
| 拖拽排序 | 被拖项跟随指针，其余项到新位置 | 160 ms | 键盘有等价排序命令，取消回原位 |
| Tooltip | opacity 0→1 | 100 ms | 延迟出现初值 500 ms，不获得焦点 |

浮层退出可统一 120—160 ms，不用复杂退出路径拖延下一操作。
排序动画不改变数据身份，列表 key 不能用当前位置代替稳定标识。
虚拟列表只处理可见项，不能为一万行同时创建布局动画。
搜索结果每次输入不整页飞入，保留稳定容器并更新结果与加载状态。

## 6. 画布与内容编辑

画布平移、节点拖动和框选直接跟随输入，不用缓动追赶指针。
缩放围绕指针或明确焦点，按钮缩放可用 160 ms，滚轮缩放优先即时响应。
节点选中只变化边框与控制点，100 ms；避免节点整体弹跳导致连线误判。
连线预览即时跟随，连接成功轻微颜色反馈 100 ms，不播放长路径流光。
AI 新增节点采用 160 ms 淡入并保留来源标记，用户仍可继续编辑其他节点。
AI 批量修改只对可见变化做摘要反馈，不自动把视口拉离用户当前位置。
撤销恢复真实数据后轻淡变，不能仅反向动画却未恢复命令结果。
加载图片先固定目标尺寸，资源到达后淡入 100 ms，减少布局跳动。
文本光标、输入法组合输入和选择范围不参与装饰动画。

## 7. 通知与 AI 状态动效

| 状态与触发 | 可见变化 | 初始时长 | 规则 |
| --- | --- | --- | --- |
| 通知进入 | opacity 0→1，y 8→0 | 220 ms | 新通知不抢焦点 |
| 通知堆叠 | 旧卡片移到新位置 | 160 ms | 不抖动全文，重要信息在记录保留 |
| queued 排队 | 静态队列图标及等待原因 | 100 ms | 不显示运行转圈冒充执行 |
| running 执行 | 低强度活动指示及当前工具 | 160 ms | 只持续于真实执行状态 |
| streaming 输出 | 文本逐批更新，光标可轻闪 | 不逐 token 补间 | 合并更新，用户上滚后不强制追底 |
| verifying 验证 | 验证图标和检查项变化 | 100 ms | 不能提前显示成功绿勾 |
| waiting 等待 | 静态等待图标及原因 | 100 ms | 停止活动指示，突出所需操作 |
| stopping 停止中 | 停止请求标记 | 100 ms | 不立即假装调用已取消 |
| failed 失败 | 错误图标与具体说明 | 100 ms | 不震动整屏，不循环闪红 |
| unknown 结果未知 | 警告标记与核查入口 | 100 ms | 与普通可重试失败区分 |
| success 成功 | 验证通过后图标切换 | 160 ms | 不用庆祝动效遮住产物 |

未知总量使用不定进度或步骤说明，不能伪造 1% 到 99% 的计时进度。
已知字节数、文件数量或确定步骤可展示真实进度，来源变化时解释估算性质。
流式消息按渲染批次更新，不对每个 token 做高度、滚动和布局动画。
任务动态岛显示简明状态，可展开详情；状态改变不反复强制展开。
错误通知保留到用户处理或在持久任务记录可找回，不能只闪过几秒。

## 8. 减少动态效果与透明度

尊重系统减少动态效果偏好，并提供应用内覆盖，优先级与设置来源可见。
交互动效可关闭参考 [W3C 2.3.3](https://www.w3.org/WAI/WCAG22/Understanding/animation-from-interactions.html)，该条为 AAA；这里将其作为产品要求，不据此宣称已经达到完整 WCAG 等级。
减少动态效果将空间移动、缩放和弹性映射为即时切换或不超过 100 ms 淡变。
拖动、滚动和画布直接操作仍跟随输入，减少的是装饰补间而不是功能。
持续旋转可替换为静态执行图标和文字状态，仍明确任务未结束。
减少透明度独立映射材质填充，与动效开关互不覆盖。
两种设置同时开启时检查层级、焦点和菜单可辨识性。
动画不能使用高频闪烁，错误强调优先图标、边界和文字。

## 9. Spectrum 组件映射

| 参考组件 | OpenArc 用途 | 必须适配内容 |
| --- | --- | --- |
| Command Search | 全局搜索 | 权限过滤、键盘、中文输入和空状态 |
| Animated Drawer | 设置及任务详情 | 焦点规则、窄窗口和中断 |
| Dynamic Island | 全局 AI 任务状态 | 真实事件、多任务摘要、结果未知 |
| Toast Stack | 系统通知 | 重要错误持久化、键盘可达 |
| Tree Nav | 文件与设置导航 | 权限过滤、大量节点和即时展开 |
| Animated Switch | 外观与连接设置 | 保存失败恢复、禁用原因 |

映射指参考方向，实际组件代码、许可、依赖和 React 兼容性仍需 D1 核验。
不宣称免费商用，不因引入参考组件而绕过自有权限与状态合同。

## 10. 原生边界与性能验证

参考 [Electron WebContentsView](https://www.electronjs.org/docs/latest/api/web-contents-view)核验网页原生视图行为。
Windows/macOS 背景效果分别验证，CSS 模糊不代表宿主壁纸原生 blur。
首选 transform 与 opacity；布局、阴影和 blur 动画需证明成本可接受。
不持续动画大范围 blur 半径，玻璃材质通常保持稳定，仅改变轻量前景状态。
初始性能目标趋近 60 fps，不是已实现事实；D1 冻结设备和测量方法。
测量固定五窗口、真实网页、一千节点画布、浅深主题和玻璃开关两组。
记录帧时间分布、长卡顿、输入响应和峰值内存，不能只截平均帧率。
在 60 Hz 与可用高刷新屏分别检查，但不要求用高刷新设备掩盖普通设备问题。
自动化验证状态、终值、焦点与中断；人工验证节奏、层级、可读性及操作跟手。
减少动态效果模式同时作为稳定截图回归条件，正常动效另做连续录屏证据。

## 11. 验收与交付合同

交付命名提案：MS-01-Tokens、MS-02-InteractionMatrix、MS-03-Prototype、MS-04-Performance、MS-05-Accessibility。
每段原型注明触发、前态、后态、token、取消行为、键盘规则和真实业务事件来源。

| 验收 ID | 通过条件 | 责任 |
| --- | --- | --- |
| MS-A01 | 组件使用统一时长曲线，例外有依据 | 设计、前端 |
| MS-A02 | 连续快速开关十次无跳变、残影或幽灵点击 | 前端、测试 |
| MS-A03 | 动画前后焦点正确，菜单可立即键盘操作 | 测试 |
| MS-A04 | 原生网页最大化、裁剪、遮挡与点击同步 | 前端、运维 |
| MS-A05 | 流式输出无逐 token 布局补间和强制抢滚动 | 前端、测试 |
| MS-A06 | 未知进度无假百分比，成功严格来自验证 | 后端、前端、测试 |
| MS-A07 | 减少动态与减少透明度可独立组合且功能完整 | 设计、测试 |
| MS-A08 | 固定环境性能报告含连续记录和降级效果 | 前端、测试 |
| MS-A09 | 停止/反向从当前值继续，旧回调不覆盖新状态 | 前端、测试 |

未取得实机与自动化证据前，所有动效仍标提案，不标已通过。
