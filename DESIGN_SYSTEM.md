# OpenArc OS 设计语言系统

版本：设计提案 0.2，2026-09-10。本文用于设计与实现，不代表组件与实机验收已完成。
用户已确认 Apple 半透明磨砂玻璃质感与 [Spectrum UI](https://ui.spectrumhq.in/) 交互方向。
以下色值、尺寸、透明度和模糊值为初始工程提案，不是 Apple 官方参数，也不是用户逐项批准值。
设计管理岗在 D1 验证后冻结首个版本，不改变既有产品范围。

0.2 修订：色彩体系由"中性灰阶 + 蓝色强调"改为**中性灰阶单色体系**，参照 Apple 官网的无彩色做法。
删除 accent 蓝色及一切彩色品牌强调；禁止使用发丝线框作为分层手段；红、绿、琥珀收缩为极少数语义色并列入白名单。
详见第 3 节和第 3.1 节。

## 1. 设计原则与视觉身份

内容优先：文件、画布和任务结果最清晰，系统外壳提供层次而不抢占注意力。
玻璃表达窗口与层级关系，不能让所有卡片都透明、发光或叠加模糊。
桌面感来自可管理的窗口、稳定工具位置、焦点和文件归属，不靠仿制品牌标志。
整体以黑白灰构成，参照 Apple 官网：靠明度差、留白、阴影和玻璃材质建立层次，不靠彩色。
不使用任何彩色品牌强调色，蓝色一律不用。
默认一个区域只有一个主要操作；主要操作用近黑或近白实心底表达，不用彩色底。
危险动作与普通按钮拉开明度、位置和间距，不靠颜色单独区分。
同一种状态在桌面、应用和扩展中心使用相同颜色、图标和措辞。
文案使用中文白话，首次出现 MCP 或 Skill 时有简短用途说明。
不使用“智能处理中”掩盖具体问题；明确显示等待模型、等待设备或验证文件。
装饰不得暗示未实现能力，演示状态必须可辨认。

## 2. Token 分层与命名

Token 是集中定义的设计值，页面不随意填写独立颜色和尺寸。
基础层保存原始色阶、间距、圆角、字体和材质参数。
语义层表达用途，例如 text.primary、surface.window、border.focus、status.failed。
组件层表达例外，例如 button.primary.background、window.chrome.height。
浅深主题只替换语义映射，业务页面不写“深色时用某十六进制颜色”的分支。
透明度降低、高对比和密度模式同样通过映射覆盖，避免各页单独修补。
组件覆盖必须说明原因、影响状态和验收证据，不能复制一套私有 token。
命名提案采用 category.role.state，例如 surface.menu.default、text.action.disabled。
颜色不写入业务状态枚举，业务状态转换成语义 token。
设计稿与实现导出维护同一版本，变更记录列出受影响组件。

## 3. 颜色初始映射

灰阶取 Apple 公开界面常用中性值，语义色取 Apple 系统色近似值，仍需按合成背景实测对比。

| 语义 | 浅色初始值 | 深色初始值 | 用途 |
| --- | --- | --- | --- |
| background.desktop | #F5F5F7 | #000000 | 无壁纸时桌面底色，**深色保持纯黑** |
| surface.window | 白色 68—80% | **#171717 @ 50%**（合成 ≈#0C0C0C） | 窗口外壳（深灰磨砂玻璃） |
| surface.content | #FFFFFF | **#171717 @ 60%**（合成 ≈#0E0E0E） | 内容编辑区（比窗口外壳略实） |
| surface.raised | #FFFFFF | **#171717 @ 72%**（合成 ≈#111111） | 卡片、菜单、通知等抬起表面 |
| surface.sunken | #E8E8ED | 黑 28% 叠加 | 表格头、轨道、凹槽区域 |
| surface.bar | 白色 58—72% | **透明（alpha 0）**，仅保留模糊 | 顶栏、Dock（用户指定透明度 0） |
| surface.pill | 白色 58% | **#171717 @ 72%**（合成 ≈#111111） | 桌面 AI 胶囊等仍需底板的浮层 |
| surface.titlebar | 黑 3.5% 叠加 | **透明（无底色）** | 窗口标题栏，与窗口外壳同值，不形成材质分区 |

> 深色所有抬起表面共用**同一基色 #171717**，层级只由 alpha 决定（0 / 0.50 / 0.60 / 0.72），
> 合成于纯黑桌面之上后依次为 #000000（顶栏/Dock）→ #0C0C0C（窗口外壳）→ #0E0E0E（内容区）→ #111111（浮层）。
>
> **顶栏与 Dock 无底色**（alpha 0），只保留 `backdrop-filter`。在纯黑桌面上这意味着底板不可见、
> 只有图标与文字悬浮；当窗口滑到其下方时才通过模糊显现。这是刻意的极简选择，
> 代价是顶栏/Dock 在纯黑区域没有任何边界——"减少透明度"模式下回落实色 #111111 以保证可辨识。
>
> **桌面 AI 胶囊不跟随此规则**：它与顶栏共用过 `--bar`，若一并透明则纯黑上只剩一行裸字，
> 因此拆出独立 token `--pill` 保留 72% 底板。
| text.primary | #1D1D1F | #F5F5F7 | 正文、标题 |
| text.secondary | #6E6E73 | #98989D | 次级描述、时间 |
| text.placeholder | #86868B | #6E6E73 | 输入提示，仍需测对比 |
| text.disabled | #A1A1A6 | #6E6E73 | 不可用说明，不用于正常正文 |
| accent.solid | #1D1D1F | #F5F5F7 | 主要动作实心底、选中标记 |
| accent.onSolid | #FFFFFF | #000000 | 实心强调按钮上的文字 |
| border.subtle | #D2D2D7 / 60% | 白 14% | 仅控件必要边界，不用于装饰 |
| border.focus | #1D1D1F | #FFFFFF | 键盘焦点环 |

深色主题**桌面底色为纯黑 #000000**，但所有抬起表面采用**深灰磨砂玻璃**。
窗口边界靠"深灰表面与纯黑桌面"的明度差建立，不使用描边，也不需要靠描边补可见性。
深灰层级由暗到亮：桌面 #000000 → 内容 #262629 → 窗口外壳 #2D2D30 → 卡片/浮层 #333336 → 顶栏与 Dock #3A3A3E。
窗口标题栏在窗口外壳基础上叠加白 5%，形成材质分区；该分区是明度差，不是分隔线。
| status.success | #0F7B34 | #30D158 | 已验证、已完成 |
| status.warning | #8A4B00 | #FFD60A | 等待、需注意、未完成 |
| status.failed | #D70015 | #FF453A | 失败、错误、危险动作 |

表中候选色不保证在任意半透明背景上满足对比，必须按最终合成背景测量。
成功、警告和失败同时配文字或图标，不以红绿区别作为唯一信息。
状态色不用于大面积底色、渐变、发光或图标容器的装饰填充。
被选中项使用背景明度、标记和文字权重共同表达，选中不等于获得键盘焦点。
链接在需要区分正文时保留下划线或明确交互样式，链接不使用蓝色。

### 3.1 色彩使用边界

允许的非中性色只有三类，且必须成对出现"图标或标记 + 文字说明"：

| 语义色 | 只允许用于 | 禁止用于 |
| --- | --- | --- |
| 失败红 | 错误、失败、危险动作、权限被拒、连接失败 | 主操作按钮、选中态、品牌标识、进度条 |
| 成功绿 | 已验证、已完成、设备在线 | 进行中、普通信息、主操作按钮 |
| 警告琥珀 | 等待用户、等待设备、结果未知、需注意 | 默认状态、装饰、主操作按钮 |

除此之外全部使用中性灰阶：正文、标题、按钮、图标、链接、选中态、进度、图表默认序列一律黑白灰。
禁用清单：任何蓝色（含链接蓝、焦点蓝、渐变蓝、壁纸蓝）、紫色、青色、品红等彩色强调；彩色渐变按钮；彩色发光与辉光；用色相区分同类信息。
平台约定例外：macOS 窗口左上角的红黄绿关闭按钮沿用系统习惯，不作为产品语义色；Windows 端按系统布局绘制。
**顶栏红绿灯**（2026-09-10 新增）同属此例外：位于顶栏左侧、字标之前，控制当前聚焦窗口，
颜色沿用系统 `#FF625B / #FFBF3E / #2BC94E`，不可借用这三个色表达产品语义（成功/失败/警告）。
无可控窗口时三个按钮整体置灰 `rgba(125,125,125,0.5)`，保持形状与位置不跳动。
第三方应用入口（例如 Adobe）的品牌色按品牌资产单独评估，不因此给系统界面引入彩色。

### 3.2 禁止发丝线框

不用 1 px 描边充当主要分层手段。Apple 官网的做法是靠背景明度差、留白和阴影分段，本项目沿用。
分层优先顺序：背景明度差 → 留白与间距 → 阴影 → 内部分隔使用 1 px 中性分隔线且限用于表单控件边界和表格线。
卡片、面板、菜单、通知、窗口默认不描边；需要边界感时用 surface 明度差加阴影表达。
禁止在深色主题使用任何浅色半透明描边制造"玻璃描边"效果，包括 rgba(255,255,255,0.08) 一类的发丝线。
控件边界（输入框、复选框、分段控件）允许 1 px border.subtle，这是可识别性要求，需在合成背景上测到 3:1。
焦点环使用 2 px 中性高对比轮廓加 2 px 间隔，不使用彩色。

**纯黑桌面的边界处理**：深色桌面为 #000000，黑色投影在纯黑上作用有限，
因此窗口、浮层与卡片一律使用深灰磨砂玻璃表面（基色 #171717，alpha 50%—72%），靠明度差建立可辨识边界。
允许在玻璃表面顶部加 `inset 0 1px 0` 内高光（白 8%—18%）表达玻璃厚度与聚焦层级，
活动窗口强于非活动窗口；这是材质高光，不是描边，不使用 `border`。

**桌面环境光上限**：桌面上的装饰光晕与环境渐变（含 `.dark::before` 装饰球与径向渐变）
合成后亮度**不得高于窗口外壳的合成值**（≈13/255）。实测若装饰球仍用白 6%，桌面右上角可达 12，
与窗口 13 几乎重合，窗口在光晕区域内会整体消失。当前限定：径向渐变白 ≤3%，装饰球 ≤2.2%。

## 4. 玻璃材质分层

| 材质 | 浅色填充 | 深色填充 | blur 初始范围 | 使用位置 |
| --- | --- | --- | --- | --- |
| M0 内容 | 白色 94—100% | #171717 @ 60% | 0—4 px | 编辑器、表格正文、画布属性 |
| M1 窗口外壳 | 白色 68—80% | #171717 @ 50% | 24—34 px | 窗口主体、标题栏、侧栏 |
| M2 浮层 | 白色 90—96% | #171717 @ 72% | 32—44 px | 菜单、搜索、通知 |
| M3 桌面栏 | 白色 58—72% | 无填充（alpha 0），仅模糊 | 36—48 px | Dock、顶部状态区域 |

深色主题桌面为纯黑，玻璃材质一律采用基色 #171717 的半透明深灰（50%—72%）。
深灰玻璃与纯黑桌面之间的明度差是窗口边界的主要来源，避免"纯黑表面叠纯黑桌面"导致窗口不可辨识。
材质填充一律中性，玻璃层不得带色相，避免叠加后偏蓝或偏紫。
M0 保证长文和精密编辑稳定；M2 比 M1 更不透明，避免多层背景叠出噪声。
同一区域最多使用一层负责模糊的主要表面，子卡片以明度差和阴影分层，不靠描边。
窗口不描边；需要边界时优先用阴影，其次用 1 px 中性内阴影高光，不使用彩色或明显描边线。
窗口阴影初始为垂直偏移 12 px、模糊 40 px、黑色 16%；深色按 28% 试测。
浮层阴影初始偏移 8 px、模糊 24 px、黑色 18%；禁止任何彩色辉光、外发光或彩色投影。
未激活窗口降低强调和阴影，不把正文降低到难以阅读。
减少透明度模式将材质映射为接近实色，保留边框和层级，不只是关闭 blur。
CSS backdrop-filter 只能描述网页合成层，不能宣称已经模糊宿主系统壁纸。
Windows/macOS 原生背景效果需分别验证支持版本、容器限制和性能；不支持时使用内部实色或玻璃替代。
浏览器原生视图的遮挡、裁剪和玻璃叠加必须通过 D1，不靠设计稿假定可实现。

## 5. 字体、字阶与内容

字体沿用 macOS 系统界面字体，不打包字体文件，也不假定 Windows 安装了 Apple 字体。
字体栈顺序：`-apple-system`、`BlinkMacSystemFont`、`SF Pro Text`、`SF Pro Display`、`Helvetica Neue`、
`PingFang SC`、`Hiragino Sans GB`、`Microsoft YaHei UI`、`Microsoft YaHei`、`sans-serif`。
`-apple-system` 与 `BlinkMacSystemFont` 在 macOS 上解析为系统 SF Pro；Windows 回退到 Microsoft YaHei UI。
代码和日志使用 `ui-monospace`、`SFMono-Regular`、`Consolas`、`monospace`。

**字重克制**：不使用 600 及以上字重，也不用浏览器默认的 bold(700)。
标题与强调统一 500（Medium），正文、按钮、导航、次级说明统一 400（Regular）。
`h1`—`h4` 与 `strong`、`b` 必须显式声明字重，避免浏览器默认加粗破坏字阶。

| 用途 | 字号 / 行高初始值 | 字重 |
| --- | --- | --- |
| 页面主标题 | 26 / 34 px | 500 |
| 区域标题 | 17 / 24 px | 500 |
| 窗口标题 | 13 / 18 px | 400 |
| 正文与表单 | 14 / 22 px | 400 |
| 按钮与导航 | 13 / 18 px | 400 |
| 次级说明 | 12 / 18 px | 400 |
| 日志 | 12 / 18 px | 400 |

标题使用负字距：主标题约 -0.02em，桌面大字标题约 -0.06em，跟随 SF Pro Display 的排版习惯。
开启 `-webkit-font-smoothing: antialiased`，避免深色背景下中文字重被渲染得更粗。

关键错误与权限说明不缩小到脚注尺寸；较长中文允许换行。
数字状态、时间和表格列可采用等宽数字，减少更新时抖动。
文件名中间截断时提供完整名称入口；路径、密钥脱敏文本和按钮不能互相混淆。
用户可放大界面；至少在 100%、125%、150% 和 200% 检查关键流程。

## 6. 空间、尺寸与密度

基础间距以 4 px 为单位，常用 4、8、12、16、24、32、48 px。
同组控件间距 8 px，表单字段间距 16 px，区域间距 24 px 作为初始值。
圆角初始：小控件 8 px、卡片 12 px、窗口 16 px、浮层 14 px、Dock 20 px。
图标采用同一风格与笔画体系，常用 16、20、24 px，禁止混用不同视觉重量。
**应用图标保留 macOS 原生彩色**（2026-09-10 定）：Dock 与系统内的应用图标沿用本机提取的原生 icns，
不去色、不加灰度滤镜。理由同 3.1 节的"平台约定例外"——Apple 官网自身也使用彩色 App Store 图标，
彩色只出现在"应用身份"这一层，界面控件、选中态、进度、图表一律中性灰阶。
若日后要统一去色，唯一改动点是给 `.dock-icon` / `.app-icon` 加 `filter: grayscale(1) saturate(.6)`，一行可逆。
图标按钮必须有可访问名称，悬停提示不能代替唯一标签。
舒适密度行高初始 40 px，紧凑密度 32 px；表单主控件初始 36 px。
点击区域建议至少 32×32 px，密集小图标扩大透明命中区域并验证不重叠。
桌面以窗口可用宽度调整布局，不把手机页面断点直接当桌面规范。
宽窗口 ≥1200 px：导航、内容、详情三栏；800—1199 px：详情可收起。
窄窗口 560—799 px：保留核心内容和抽屉；小于应用最小宽度时限制缩放或专门适配。
窗口默认最小尺寸提案 560×400 px，画布和浏览器按可用性单独验证。
最低桌面验证尺寸初始 1280×720，另测 1440×900、1920×1080 与高像素密度屏幕。

## 7. 层级、焦点与原生视图

逻辑层顺序：桌面背景、普通窗口、活动窗口、临时菜单、模态对话框、系统锁屏。
通知不得覆盖锁屏敏感内容；任务动态岛不能挡住系统关键操作。
具体 z-index 由窗口管理器集中分配，应用不得自设无限大的层级。
网页 DOM 层级与 WebContentsView 原生视图层级分别管理，不能用更大 z-index 解决所有遮挡。
参考 [Electron WebContentsView](https://www.electronjs.org/docs/latest/api/web-contents-view) 与 [安全说明](https://www.electronjs.org/docs/latest/tutorial/security)验证边界。
菜单打开后支持方向键和 Escape；关闭后焦点返回触发者。
对话框限定焦点范围，背景不可误操作；普通非模态侧栏不抢走当前输入焦点。
焦点轮廓始终可见，初始 2 px 外环及 2 px 间隔，使用中性高对比色，必须与合成背景有足够区分。

## 8. 核心组件状态合同

组件交付至少覆盖默认、悬停、按下、键盘焦点、禁用；异步组件另含加载、错误和成功。

| 组件 | 必须额外覆盖的状态与行为 |
| --- | --- |
| Button | 主要/次要/危险；加载防重复但保留名称 |
| IconButton | 可访问名称、提示、焦点、禁用原因 |
| Input/Textarea | 空、输入、校验错误、只读、密钥脱敏 |
| Select/Combobox | 搜索、无结果、加载、键盘选择 |
| Switch/Checkbox | 开关、混合选择、保存中、保存失败回显 |
| Tabs | 激活、溢出、关闭、未保存标记 |
| Menu/Popover | 分组、快捷键、子菜单、边缘避让 |
| Dialog | 标题、正文、主要操作、取消、焦点恢复 |
| Drawer | 展开收起、窄窗口、非模态与模态区别 |
| TreeNav | 展开、选中、焦点、无权节点过滤 |
| Table/List | 排序、多选、分页、空状态、加载、冲突 |
| AppCard | 未安装、安装中、可用、更新、停用、失败 |
| WindowChrome | 活动、非活动、最小化、最大化、未保存 |
| DockItem | 未运行、运行、多窗口、启动失败 |
| Toast/Notification | 信息、成功、失败、可行动、长驻 |
| TaskCard | 排队、执行、等待、验证、停止、失败、完成 |
| Progress | 已知总量、未知总量、暂停、错误 |
| CanvasNode | 选中、编辑、连接中、只读、生成中、资源失效 |

禁用控件不应隐藏原因，权限不足不显示敏感数据来解释原因。
加载骨架模拟结构而非虚构内容；失败保留用户输入与可恢复入口。
通知自动消失前不承担唯一错误信息，任务记录保留重要结果。

## 9. 页面映射与 AI 可见信息

登录使用 M1 卡和 M0 表单；桌面使用 M3 栏；文件及设置使用 M1 导航加 M0 内容。
浏览器与画布内容保持完整可读，玻璃集中在工具栏和系统外壳。
Skill、MCP 和插件中心共享列表、详情、依赖和状态组件，避免三套管理风格。
AI 区域展示任务目标、目标设备、当前动作、等待原因、产物和验证结果。
不展示模型内部思维链；公开步骤摘要由结构化事件生成并脱敏。
结果未知与失败分开呈现：前者要求核查实际副作用，不能直接提供“安全重试”承诺。
成功标记只对应验证通过，流式文本结束不能触发任务成功样式。
停止请求后显示停止中，确认后显示已停止，并注明已完成动作保留。
设备、账号和项目归属在操作前可见，不能只在错误后才解释执行位置。

## 10. 可访问性与平台差异

普通文本对比初始验收门槛为 4.5:1，大字为 3:1，必要控件边界和状态标记为 3:1。
依据 [W3C 文本对比说明](https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html)与[非文本对比说明](https://www.w3.org/WAI/WCAG22/Understanding/non-text-contrast.html)。大字为至少 24 CSS px，或约 18.67 CSS px 粗体；3:1 控件要求针对必要识别信息，不要求所有装饰线达到该值。
大字按可访问性标准定义识别，不把所有 18 px 中文当作大字降低要求。
测量最终合成背景、浅深主题、不同壁纸及透明度模式，候选颜色不是通过证据。
减少动态效果与减少透明度是两个独立设置，可分别开启并叠加。
所有核心流程可用键盘完成，焦点不被动画隐藏或突然跳到背景。
macOS 窗口控制与快捷键遵循平台习惯，Windows 使用对应系统布局与 Control 键提示。
平台原生窗口控制由桌面实现核验，不能只绘制三个圆点却没有对应行为。
系统高对比、显示缩放与字体差异分别测试，无法读取系统偏好时提供应用内设置。

## 11. 交付文件与验收

设计交付命名提案：DS-01-Tokens、DS-02-Materials、DS-03-Components、DS-04-Pages、DS-05-Accessibility。
每份交付注明版本、主题、密度、窗口尺寸、状态、平台和对应任务 ID。
组件例外集中登记，不让页面局部修补成为默认规范。

| 验收 ID | 通过条件 | 责任 |
| --- | --- | --- |
| DS-A01 | 浅深主题所有核心语义 token 有映射 | 设计、前端 |
| DS-A02 | 四层材质在三种壁纸与减少透明度下可读 | 设计、测试 |
| DS-A03 | 18 类组件具备所列交互与异常状态 | 前端、测试 |
| DS-A04 | 合成背景文本和控件对比测量通过 | 设计、测试 |
| DS-A05 | 键盘遍历、焦点恢复及放大流程通过 | 测试 |
| DS-A06 | 原生网页不穿透菜单或锁屏 | 前端、运维 |
| DS-A07 | AI 状态来自真实事件，未知结果不标成功 | 后端、前端、测试 |
| DS-A08 | Windows/macOS 实机差异分别记录 | 运维、测试 |
| DS-A09 | 全量界面无蓝色与彩色强调，无装饰性发丝线框；语义色仅出现在白名单场景且均配文字或图标 | 设计、前端、测试 |

Spectrum 实际组件采用、依赖和许可仍待核验，不宣称免费商用。
本文件是实施依据，未完成视觉实现或可访问性测试前不得标记已通过。
